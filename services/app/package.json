{
  "name": "repo-agent-launcher",
  "version": "1.0.0",
  "description": "Repo Agent Launcher UI — manage Git/Agent credentials, allocate ttyd slots and launch coding agents",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
